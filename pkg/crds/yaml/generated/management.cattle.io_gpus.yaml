---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: gpus.management.cattle.io
spec:
  group: management.cattle.io
  names:
    kind: GPU
    listKind: GPUList
    plural: gpus
    singular: gpu
  scope: Namespaced
  versions:
  - name: v3
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              enabled:
                description: norman:"nullable" norman 规范
                type: boolean
            required:
            - enabled
            type: object
          status:
            properties:
              clusterGPUInfo:
                description: 每个集群的 GPU 信息
                items:
                  description: 表示每个集群的 GPU 信息
                  properties:
                    clusterId:
                      description: 集群 ID
                      type: string
                    nodeGPUInfo:
                      description: 当前集群中所有节点的 GPU 信息
                      items:
                        description: 表示每个节点的 GPU 信息
                        properties:
                          nodeHostName:
                            description: 节点主机名
                            type: string
                          nodeId:
                            description: 节点 ID
                            type: string
                          nodeName:
                            description: 节点名称
                            type: string
                          totalGPU:
                            description: 总 GPU 数量
                            type: integer
                          unusedGPU:
                            description: 未使用的 GPU 数量
                            type: integer
                          usedGPU:
                            description: 已使用的 GPU 数量
                            type: integer
                        required:
                        - nodeHostName
                        - nodeId
                        - nodeName
                        - totalGPU
                        - unusedGPU
                        - usedGPU
                        type: object
                      type: array
                    totalGPUCount:
                      description: 当前集群中所有节点的 GPU 总量
                      type: integer
                  required:
                  - clusterId
                  - nodeGPUInfo
                  - totalGPUCount
                  type: object
                type: array
              message:
                type: string
              totalGPUCount:
                description: 所有集群中所有节点的 GPU 总量
                type: integer
            required:
            - clusterGPUInfo
            - message
            - totalGPUCount
            type: object
        required:
        - spec
        - status
        type: object
    served: true
    storage: true
